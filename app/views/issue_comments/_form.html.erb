<%= form_with(model: issue_comment, local: true) do |form| %>
  <% if issue_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_comment.errors.count, "error") %> prohibited this issue_comment from being saved:</h2>

      <ul>
      <% issue_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.number_field :comment, id: :issue_comment_comment %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :issue_comment_user_id %>
  </div>

  <div class="field">
    <%= form.label :issue_id %>
    <%= form.text_field :issue_id, id: :issue_comment_issue_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: sprint, local: true) do |form| %>
  <% if sprint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sprint.errors.count, "error") %> prohibited this sprint from being saved:</h2>

      <ul>
      <% sprint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from %>
    <%= form.date_select :from, id: :sprint_from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.date_select :to, id: :sprint_to %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sprint_name %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :sprint_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
